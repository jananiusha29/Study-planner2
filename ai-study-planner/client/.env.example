# Copy this file to .env for local dev, or set the same variable in your
# hosting provider's dashboard when deploying.

# Base URL of the deployed/local API server (no trailing slash)
VITE_API_URL=http://localhost:4000
